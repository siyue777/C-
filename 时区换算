#include <stdio.h>

int main()
{
	int BJT = 0;
	int UTC = 0;

// 输入时间
	scanf("%d", &BJT); 

// 判断时间 
	if (BJT <800 )
	{
		UTC = (1600+BJT);
		printf("%d", UTC);
 	} 
	else
	{	UTC =(BJT-800);
		printf("%d", UTC);
	}
	return 0;
}
